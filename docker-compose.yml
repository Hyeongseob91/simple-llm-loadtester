services:
  api:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    container_name: llm-loadtest-api
    ports:
      - "8085:8085"
    volumes:
      - ./data:/data
    environment:
      - DATABASE_PATH=/data/benchmarks.db
      - PORT=8085
    extra_hosts:
      - "host.docker.internal:192.168.123.35"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8085/health')"]
      interval: 30s
      timeout: 10s
      retries: 3

  web:
    build:
      context: .
      dockerfile: services/web/Dockerfile
    container_name: llm-loadtest-web
    ports:
      - "5050:5050"
    environment:
      - API_URL=http://api:8085
    depends_on:
      - api
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:5050"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  data:
